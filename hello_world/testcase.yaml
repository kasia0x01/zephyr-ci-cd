tests:
  sample.hello_world:
    timeout: 10
    tags: introduction
    platform_allow: qemu_cortex_m3
    harness: console
    harness_config:
      type: one_line
      regex:
        - "Popped 5 elements"